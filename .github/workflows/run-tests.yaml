name: Integration Tests

on:
  push:
    branches: [ "master" ]
  pull_request:

permissions:
  contents: read

jobs:
  bats:    
    name: bats-${{ matrix.os }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:        
        os: [centos, focal]

    steps:
    - uses: actions/checkout@v3
        
    - name: "Simulate tests"
      run: |        
        echo "Running on ${{ matrix.os }}"
